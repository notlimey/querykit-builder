# `changeset publish` shells out to `pnpm publish`, which otherwise refuses to
# run from CI's detached HEAD ("branch is not on main") — the release workflow
# already gates publishing on a merge to main.
git-checks=false
